# Infrastructure

This folder is reserved for:
- Dockerfiles
- compose overrides
- Nginx or reverse-proxy configs
- deployment manifests
- environment-specific helpers

The goal is to keep deployment concerns outside product code.
